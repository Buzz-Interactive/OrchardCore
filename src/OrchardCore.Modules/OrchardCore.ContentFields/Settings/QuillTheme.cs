using System.ComponentModel.DataAnnotations;

namespace OrchardCore.ContentFields.Settings
{
  public enum QuillTheme
  {
    [Display(Name = "Snow")]
    Snow = 10,
    [Display(Name = "Bubble (minimal)")]
    Bubble = 20
  }
}
