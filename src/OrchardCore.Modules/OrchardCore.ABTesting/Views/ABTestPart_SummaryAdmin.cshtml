@model ABTestPartViewModel

@if (Model.IsActive)
{
    <span class="badge bg-success">@T["Active"]</span>
}
else
{
    <span class="badge bg-secondary">@T["Inactive"]</span>
}

<span class="badge bg-info">@T["A: {0}% / B: {1}%", Model.PercentageA, Model.PercentageB]</span>

